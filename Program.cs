using System;

namespace line_comparison_problem
{
    class Program
    {
        static void Main(string[] args)
        {
            int x1, x2, y1, y2;
            int z1, z2;
           z1= Math.Sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1));
            Console.WriteLine(Math.Sqrt);
        }
    }
}
